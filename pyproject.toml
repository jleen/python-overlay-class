[project]
name = "overlay"
version = "0.1.0"
description = ""
authors = [{name = "John Leen", email = "jleen@saturnvalley.org"}]
license = {text = "MIT"}
requires-python = ">=3.8"

[dependency-groups]
dev = ["pytest>=8.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
